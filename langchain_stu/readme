pip install Werkzeug==3.0.0 Flask-Login==0.6.3 Flask==3.0.0
pip install --upgrade typing-extensions

pip install "langserve[all]"
pip install "langserve[all]"

pip install -U langchain-cli langchain_openai langchain_community dashscope

pip install redis
pip install faiss-cpu

pip install beautifulsoup4
